{
  "name": "nulltest",
  "version": "0.0.1",
  "license": "MIT",
  "devDependencies": {
    "nullstack": "~0.20.3",
    "tailwindcss": "^3.0.0",
    "postcss-loader": "^7.0.1"
  },
  "scripts": {
    "start": "npx nullstack start",
    "build": "npx nullstack build",
    "vercel-build": "npm run build && npx nullstack-adapt-vercel",
    "cleanup": "rm -fr .development .production node_modules package-lock.json"
  }
}
